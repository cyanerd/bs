import contract from "./contract.json";

const DOMAIN = "";

const PRODUCTION_ENV = true;

const projectConfig = {
  configUrl: "https://2.pix.my/api/getConfig/",
  updateInterval: 5 * 1000,
};

export default projectConfig;
